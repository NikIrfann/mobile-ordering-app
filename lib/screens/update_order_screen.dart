// lib/screens/update_order_screen.dart

import 'package:flutter/material.dart';
import '../order.dart';

class UpdateOrderScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Update Order'),
      ),
      body: Center(
        child: Text('Update Order Screen'),
      ),
    );
  }
}
